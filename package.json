{
  "name": "hector-ai",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "cross-env NODE_ENV=development concurrently \"parcel index.html\" \"electron .\"",
    "build": "parcel build index.html && electron-builder",
    "clean": "rm -rf dist .parcel-cache"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1",
    "@parcel/transformer-glsl": "2.15.4",
    "parcel": "^2.15.4",
    "concurrently": "^8.0.0",
    "cross-env": "^7.0.3"
  }
}
